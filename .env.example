# Copy to .env and adjust if needed. Not committed.
MONGODB_URI=mongodb://localhost:27017/lets-play

JWT_SECRET= # Generate your own, e.g.: openssl rand -base64 32

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me